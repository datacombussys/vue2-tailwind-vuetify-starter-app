#!/bin/bash

/bin/echo "The file has been loaded and run by a cronjob" 

# I need to do a search for recurring events that never end and tell Django to create new events and store them into the database
# PayCycleRecurrence, UserCalendarEvents, ShiftCycles, etc.

# I need to do a script to delete the files from Amazon S3 that are not in the Django Database